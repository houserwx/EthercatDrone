# Tests — unit + integration tests (CTest)
# Phase 1 test targets will be added here as subsystems are implemented.

# Example:
# add_executable(test_common src/test_common.cpp)
# target_link_libraries(test_common PRIVATE common)
# add_test(NAME test_common COMMAND test_common)
